/*
 * @author: Lander Buysse
 * @file: inputcontrol.h
 */

//files to include
#include <stdio.h>
#include "joystickDriver.h"
#include "buttonDriver.h"

extern int hour=0;
extern int min=0;
extern int sec=0;
extern int alarmOnOff=0;

enum timeParts{
	HOURPART=0,
	MINPART=1,
	SECPART=2,
}timePart;

enum changeState{
	STRD=0,   //standard state that displays time
	TIME=1,	  //state to set the clock
	ALARM=2,	  //state to set the alarm time
	MUSIC=3,   //state to change the music tune used
}cState;

/*
 * prototypes
 */
int getAlarm(void);
int getHour(void);
int getMin(void);
int getSec(void);
int getState(void);
char* getSetting(int);
void setTimeParts(int hourMinSec);
void setAlarm(int onOff);
void incrementTime(int hourMinSec);
void decrementTime(int hourMinSec);
void initJoystick(void);
void initSwitch2(void);

